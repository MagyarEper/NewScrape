services:
  - type: web
    name: newscrapeer
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn web.app:app
    autoDeploy: true
    envVars:
      - key: DATABASE_URL
        sync: false
